define([], function () {
    var ctrl = ['$scope', function ($scope) {
        console.log('home.sidebar.ctrl');
    }];
    ctrl.name = 'home.sidebar.ctrl';
    ctrl.registerType = 'controller';
    return ctrl;
});
